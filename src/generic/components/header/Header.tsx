//import styles from "@/generic/components/header/Header.module.scss";

export const Header = () => {
  return (
    // <header className={styles.header} role="banner">
    <header role="banner">header</header>
  );
};
